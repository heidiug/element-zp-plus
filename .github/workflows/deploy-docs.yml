name: Deploy VitePress Docs via GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: ⬇️ Checkout 仓库
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🛠️ Setup Node.js 环境 (使用 npm 缓存)
        uses: actions/setup-node@v4
        with:
          node-version: 20
          # 🚨 关键修改: 将缓存改为 npm
          cache: 'npm'

      # 🚨 移除所有 pnpm/action-setup 相关的步骤!

      - name: 📦 安装依赖 (使用 npm)
        # 🚨 关键修改: 使用 npm install 代替 pnpm install
        run: npm install

      - name: 🏗️ 构建 VitePress 文档 (使用 npm run)
        # 🚨 关键修改: 使用 npm run docs:build
        run: npm run docs:build

      # === 部署步骤保持不变 ===
      - name: 🚀 部署到 GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: docs/.vitepress/dist

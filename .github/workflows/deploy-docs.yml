name: Deploy VitePress Docs via GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: ⬇️ Checkout 仓库
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # 👇 修改: 使用 setup-node 来安装 Node.js 和 pnpm
      - name: 🛠️ Setup Node.js & pnpm 环境
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'
          # 告诉 setup-node 自动安装 pnpm
          cache-dependency-path: pnpm-lock.yaml # 假设你使用了 pnpm-lock.yaml

      # 👇 新增: 使用 pnpm action setup 来确保 pnpm 可用
      - name: 📥 Ensure pnpm is available
        uses: pnpm/action-setup@v3
        with:
          version: 8 # 推荐安装 pnpm 8 版本
          run_install: false # 不在这里运行 install

      - name: 📦 安装依赖
        # 现在 pnpm 应该可以找到了
        run: pnpm install --frozen-lockfile

      - name: 🏗️ 构建 VitePress 文档
        run: pnpm run docs:build

      # === 部署步骤 ===
      - name: 🚀 部署到 GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: docs/.vitepress/dist

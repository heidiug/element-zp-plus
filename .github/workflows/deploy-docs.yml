name: Deploy VitePress Docs via GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: ⬇️ Checkout 仓库
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # 1. 🛠️ 设置 Node.js 环境 (不安装 pnpm，只设置缓存)
      - name: Setup Node.js Environment
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm' # 启用 pnpm 缓存

      # 2. 📥 关键步骤: 明确安装 pnpm (这是解决找不到问题的核心)
      - name: Install pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 8 # 指定一个稳定的 pnpm 版本
          run_install: false # 只是安装 pnpm 工具，不运行 install

      - name: 📦 安装依赖 (使用 pnpm)
        # 现在 pnpm 路径已设置好，可以运行 install
        run: pnpm install --frozen-lockfile

      - name: 🏗️ 构建 VitePress 文档
        run: pnpm run docs:build

      # === 部署步骤保持不变 ===
      - name: 🚀 部署到 GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: docs/.vitepress/dist
